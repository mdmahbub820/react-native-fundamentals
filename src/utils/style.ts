import { StyleSheet } from "react-native";

const st = StyleSheet.create({
  externalStyle: {
    backgroundColor: "crimson",
    color: "white",
    fontSize: 20,
    padding: 20,
    margin: 20,
  },

  btnStyle: {
    backgroundColor: "#DDD",
    padding: 20,
  },
});

export default st;
